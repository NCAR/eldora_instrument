VxWorks compatible code for using the
NAT eth29fc ethernet card. the code provided
by NAT (via email), in the file eth29.tar,
was used as the starting point for this code.

A single executable is built: eth29.o.
Load this into the vxworks system as:
ld < eth29.o

